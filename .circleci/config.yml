version: 2
jobs:
  build:
    docker:
      - image: vvakame/review:3.1
    steps:
      - checkout
      - run:
          name: Build PDF
          command: rake clean pdf
          working_directory: ./sample
      - run:
          name: move book
          command: |
            chmod -R 777 /tmp
            mv ./sample/book.pdf /tmp
      - store_artifacts:
          path: /tmp/book.pdf
          destination: book.pdf
      - save_cache:
          key: build-{{ .Revision }}
          paths:
            - /tmp
  deploy:
    docker:
      - image: circleci/golang
    working_directory: ~/deploy
    steps:
      - run: go get github.com/prasmussen/gdrive
      # 成果物をリストア
      - run:
          name: tmp permisson
          command: |
            chmod -R 777 /tmp
      - restore_cache:
          keys:
            - build-{{ .Revision }}
      - deploy:
          command: |
            echo $GOOGLE_SERVICE_ACCOUNT_CREDENTIAL > credential.json
            ls /tmp
            filename="/tmp/book-"$(date +%Y%m%d_%H%M%S)".pdf"
            mv /tmp/book.pdf $filename
            gdrive --config $(pwd) --service-account credential.json upload -p ${GOOGLE_DRIVE_FOLDER} $filename

workflows:
  version: 2
  build_and_deploy:
    jobs:
      - build
      - deploy:
          requires:
            - build
